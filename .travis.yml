sudo: false

# language: cpp
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - wget

#install:
#  - ./install-lrte.sh

script:
  - export
  - whoami
  - ls $HOME
  - ps aux
  - uname -a
  - ls -l /
  - ls -l /sbin
  - sudo -E apt-get update

notifications:
  email: false
